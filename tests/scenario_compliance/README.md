# Проверка соответствия кода сценариям

> Автоматическая система проверки того, насколько код бота соответствует текстовым сценариям из `docs/testing-scenarios.md`.

## Структура

```
tests/scenario_compliance/
├── README.md                 # Этот файл
├── requirements.yaml         # ТЗ: маппинг сценариев → код
├── scripts/                  # Скрипты проверки
│   ├── __init__.py
│   ├── checker.py            # Основной скрипт
│   ├── code_analyzer.py      # Анализатор AST/regex
│   └── report_generator.py   # Генератор отчётов
└── reports/                  # Результаты проверок
    └── YYYY-MM-DD-тест-сценариев.md
```

## Быстрый старт

```bash
# Из корня проекта
python -m tests.scenario_compliance.scripts.checker

# С подробным выводом
python -m tests.scenario_compliance.scripts.checker -v

# JSON вывод (для CI/CD)
python -m tests.scenario_compliance.scripts.checker --json
```

## Цветовая индикация

| Статус | Условие | Интерпретация |
|--------|---------|---------------|
| 🟢 | Основные ≥90% **И** вспомогательные ≥80% **И** общее ≥90% | Бот работает хорошо — все сценарии работают |
| 🟡 | Основные ≥80% **И** общее ≥70% | Основные сценарии работают, вспомогательные могут не работать |
| 🔴 | Основные <80% **ИЛИ** общее <70% | Критично — основные сценарии под угрозой |

**ВАЖНО:**
- **Зелёный** — требуется, чтобы работали И основные (≥90%), И вспомогательные (≥80%) сценарии
- **Жёлтый** — требуется, чтобы работали как минимум основные сценарии (≥80%)
- **Красный** — основные сценарии не работают или общее покрытие критически низкое

## ТЗ (requirements.yaml)

Файл `requirements.yaml` содержит маппинг между сценариями и кодом:

```yaml
class_1_registration:
  name: "Регистрация и онбординг"
  scenarios:
    - id: "1.1"
      name: "Первый запуск бота"
      priority: critical  # critical или normal
      checks:
        - type: handler      # Тип проверки
          file: "bot.py"     # Файл для проверки
          name: "cmd_start"  # Имя функции/класса
          description: "Обработчик /start"
```

### Типы проверок

| Тип | Описание | Параметры |
|-----|----------|-----------|
| `function` | Наличие функции | `file`, `name` или `pattern` |
| `handler` | Обработчик с декоратором | `file`, `name` или `pattern` |
| `state` | Класс или переменная состояния | `file`, `name` или `pattern` |
| `constant` | Константа (включая `Class.ATTR`) | `file`, `name` |
| `localization` | Ключи локализации | `file`, `keys` (список) |
| `pattern` | Regex-паттерн в коде | `file`, `pattern` |

### Приоритеты

- **critical** — основные сценарии, без которых бот не работает (вес ×2)
- **normal** — вспомогательные сценарии (вес ×1)

## Примеры отчётов

Отчёты сохраняются в `reports/` с именем `YYYY-MM-DD-тест-сценариев.md`:

```
reports/
├── 2026-01-20-тест-сценариев.md
├── 2026-01-21-тест-сценариев.md
└── 2026-01-22-тест-сценариев.md
```

## Автоматический запуск

### GitHub Actions (ежедневно в 06:00 MSK)

Workflow файл: `.github/workflows/scenario-compliance.yml`

```yaml
name: Scenario Compliance Check
on:
  schedule:
    - cron: '0 3 * * *'  # 06:00 MSK = 03:00 UTC
  workflow_dispatch:     # Ручной запуск
```

### Локальный cron

```bash
# Редактировать crontab
crontab -e

# Добавить строку (06:00 ежедневно)
0 6 * * * cd /path/to/aist_track_bot && python -m tests.scenario_compliance.scripts.checker
```

## Добавление новых проверок

1. Открыть `requirements.yaml`
2. Найти соответствующий класс или создать новый
3. Добавить сценарий:

```yaml
- id: "X.Y"
  name: "Название сценария"
  priority: critical  # или normal
  checks:
    - type: function
      file: "path/to/file.py"
      name: "function_name"
      description: "Что проверяется"
```

4. Запустить проверку для валидации:

```bash
python -m tests.scenario_compliance.scripts.checker -v
```

## Интерпретация результатов

### Если покрытие упало

1. Посмотреть раздел "Непройденные проверки" в отчёте
2. Варианты действий:
   - **Код изменился** — обновить проверки в `requirements.yaml`
   - **Функция удалена** — удалить или заменить проверку
   - **Баг в коде** — исправить код

### Если проверка ложно-отрицательная

Проверки используют AST и regex, которые могут не найти:
- Динамически генерируемые функции
- Сильно изменённые имена
- Код в нестандартных форматах

В таких случаях можно:
1. Использовать `pattern` с regex вместо точного имени
2. Добавить альтернативные проверки
3. Пометить сценарий как не автоматизируемый

## Зависимости

```bash
pip install pyyaml
```

Стандартная библиотека: `ast`, `re`, `pathlib`, `dataclasses`, `datetime`, `json`, `argparse`
